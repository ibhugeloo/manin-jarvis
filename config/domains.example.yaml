# Noms de domaine suivis. Copiez en domains.yaml.
#   - name      : domaine
#     registrar : bureau d'enregistrement (OVH, Gandi, Namecheap…)
#     expires   : YYYY-MM-DD
#     price     : (optionnel) coût annuel
#     notes     : (optionnel)

domains:
  - name: example.com
    registrar: <registrar>
    expires: '2027-01-01'
    price: 12.0
    notes: ''
