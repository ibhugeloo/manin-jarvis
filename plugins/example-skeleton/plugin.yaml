# Plugin Jarvis — squelette de référence
# Copier ce dossier en `plugins/<votre-nom>/` et adapter.

name: example-skeleton
version: 0.1.0
description: Plugin squelette qui montre la convention. Désactivé par défaut.
author: your-name
type: experimental
enabled: false  # passer à true quand un vrai plugin reprend cette structure

depends_on:
  - core  # placeholder — le bootstrap V2 résoudra

commands: []
launch_agents: []
prompt_injections: []
memory: []
env_required: []

# Notes :
# - Tous les chemins sont relatifs au dossier du plugin
# - Le bootstrap V2 traitera ce yaml ; en attendant, c'est juste documentation
